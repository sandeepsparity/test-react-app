import React from 'react';
import './styles.scss';

const Header = ({ title }) => {
    return (
    <header>
        <div className='wrap'>
            <div className='logo'></div>
        </div>
    </header>
    )
}

export default Header;